// Project-level build file using modern Plugins DSL
plugins {
    id 'com.android.application' version '8.2.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
